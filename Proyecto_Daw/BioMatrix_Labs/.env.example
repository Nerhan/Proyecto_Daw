DEBUG=False
SECRET_KEY=change-me-generate-a-new-random-value
ALLOWED_HOSTS=localhost,127.0.0.1,.onrender.com
# Orígenes del frontend autorizados (separados por comas)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Si se define DATABASE_URL, tiene prioridad y se ignoran las variables DB_* de abajo.
# DATABASE_URL=postgres://user:password@host:5432/dbname

DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=change-me
DB_HOST=change-me.supabase.co
DB_PORT=5432
